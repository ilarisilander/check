""" Module with all the constants """
import os
import datetime

# PATHS
TODO_PATH = os.path.join('data', 'todo_list.json')
SETTINGS_PATH = os.path.join('data', 'todo_settings.json')


# DATES
CURRENT_DATE = str(datetime.date.today())
